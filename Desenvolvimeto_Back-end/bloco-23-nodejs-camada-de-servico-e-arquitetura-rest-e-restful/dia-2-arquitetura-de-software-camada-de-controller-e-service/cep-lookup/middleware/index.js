const error = require('./error')
const validationCep = require('./validationCep')
const validationAdress = require('./validationAdress')

module.exports = {
  error,
  validationCep,
  validationAdress
}